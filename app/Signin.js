import React, { useState } from "react";
import { Text, StyleSheet, View, TextInput, TouchableNativeFeedback, Image, ActivityIndicator, Dimensions, StatusBar, ImageBackground } from "react-native";

const Signin = (prop) => {

    return (
        <Text>签到</Text>
    );
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
        marginLeft: 49,
        marginRight: 49,
        // borderWidth: 1,
        // borderColor: "black"
    },
});

export default Signin;